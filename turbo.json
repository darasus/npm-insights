{
  "$schema": "https://turborepo.org/schema.json",
  "globalDependencies": ["$REDIS_URL"],
  "globalEnv": [
    "REDIS_URL"
  ],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    },
    "generate": {
      "cache": false,
      "env": [
        "REDIS_URL"
      ]
    }
  }
}
